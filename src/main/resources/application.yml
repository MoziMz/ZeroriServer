spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_ADDRESS}:3306/zeroplanet?autoReconnect=true&useSSL=false
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
social:
  kakao:
    url: http://127.0.0.1 #TODO FIX
    appId: 1
    adminKey: 1