```java
public class ConventionController {
    //
    // 외부 주입 서비스 (private final 붙이기)
    //
    // -------------------- -------------------- (엔티티 위주) -------------------- -------------------- //
    // 순서 -> 첫번째 기준 RCUD/ETC => 두번째 기준 (하나 -> 리스트) => 세번째 기준 (기본 -> 조건)
}
```